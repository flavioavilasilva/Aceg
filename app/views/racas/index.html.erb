<h1>Listing racas</h1>

<table>
  <tr>
    <th>Raca</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @racas.each do |raca| %>
  <tr>
    <td><%= raca.raca %></td>
		<td><%= raca.tipo_animal.tipo_animal %></td>
    <td><%= link_to 'Show', raca %></td>
    <td><%= link_to 'Edit', edit_raca_path(raca) %></td>
    <td><%= link_to 'Destroy', raca, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Raca', new_raca_path %>
