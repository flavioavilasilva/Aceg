<%= form_for(@tipo_animal) do |f| %>
  <% if @tipo_animal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_animal.errors.count, "error") %> prohibited this tipo_animal from being saved:</h2>

      <ul>
      <% @tipo_animal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo_animal %><br />
    <%= f.text_field :tipo_animal %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
