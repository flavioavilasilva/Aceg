<%= form_for(@status_animal) do |f| %>
  <% if @status_animal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status_animal.errors.count, "error") %> prohibited this status_animal from being saved:</h2>

      <ul>
      <% @status_animal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status_animal %><br />
    <%= f.text_field :status_animal %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
