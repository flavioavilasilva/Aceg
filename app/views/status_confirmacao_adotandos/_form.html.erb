<%= form_for(@status_confirmacao_adotando) do |f| %>
  <% if @status_confirmacao_adotando.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status_confirmacao_adotando.errors.count, "error") %> prohibited this status_confirmacao_adotando from being saved:</h2>

      <ul>
      <% @status_confirmacao_adotando.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status_confirmacao_adotando %><br />
    <%= f.text_field :status_confirmacao_adotando %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
