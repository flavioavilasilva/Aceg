<%= form_for(@doador_animal) do |f| %>
  <% if @doador_animal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@doador_animal.errors.count, "error") %> prohibited this doador_animal from being saved:</h2>

      <ul>
      <% @doador_animal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :usuario_id %><br />
    <%= f.number_field :usuario_id %>
  </div>
  <div class="field">
    <%= f.label :status_doador_id %><br />
    <%= f.text_field :status_doador_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
