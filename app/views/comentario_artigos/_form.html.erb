<%= form_for(@comentario_artigo) do |f| %>
  <% if @comentario_artigo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comentario_artigo.errors.count, "error") %> prohibited this comentario_artigo from being saved:</h2>

      <ul>
      <% @comentario_artigo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :autor %><br />
    <%= f.text_field :autor %>
  </div>
  <div class="field">
    <%= f.label :conteudo %><br />
    <%= f.text_area :conteudo %>
  </div>
  <div class="field">
    <%= f.label :artigo %><br />
    <%= f.text_field :artigo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
